enum Controller{
    power,
    ch_minus,
    ch_plus
}

enum OnOff{
    on,
    off
}

enum Channels{
    fuzz,
    one,
    two,
    three,
    four
}

export {
    Controller,
    OnOff,
    Channels
};